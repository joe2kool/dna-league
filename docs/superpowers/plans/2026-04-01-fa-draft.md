# Free Agent Draft — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a standalone, live-joinable Free Agent draft room (`fa-draft.html`) where members draft individual MLB The Show players from drafted teams, with a configurable rating range, reversed snake order, and a post-draft trade export.

**Architecture:** A new standalone page `fa-draft.html` (mirrors `draft.html`) backed by a new `js/fa-draft-room.js` engine. Reuses existing Supabase tables (`drafts`, `draft_slots`, `draft_picks`) with `type='player'`. A new Worker route `/fa-roster` fetches full player attributes within a rating range. Entry point is a new "FA Draft" button on each season card in `index.html`. Export is generated entirely client-side.

**Tech Stack:** Vanilla HTML/CSS/JS, Supabase JS v2, Cloudflare Worker (existing), Supabase Realtime broadcast channel

---

## File Map

| Action | File | Responsibility |
|--------|------|----------------|
| Create | `fa-draft.html` | FA draft room page — auth, config screen, draft room shell |
| Create | `js/fa-draft-room.js` | FA draft engine — state, snake order, picks, timer, realtime, export |
| Create | `css/fa-draft.css` | FA draft room styles (shares design tokens with `css/draft.css`) |
| Modify | `worker.js` | Add `/fa-roster?team=X&min=Y&max=Z` route |
| Modify | `index.html` | Add "FA Draft" button to each season card in `renderSeasonCard()` |
| Modify | `CLAUDE.md` | Document new Worker route and FA draft entry point |

---

## Task 1: Worker — `/fa-roster` route

**Files:**
- Modify: `worker.js`

The existing `/roster` route returns only the top 5 players with minimal fields. The new `/fa-roster` route returns all Live Series players for a team within an OVR range, with full attribute fields for hitters, pitchers (including pitch arsenal), and quirks.

- [ ] **Step 1: Add `/fa-roster` route to worker.js**

Open `worker.js`. After the closing `}` of the `/roster` block (line ~82), add before the `/teams` block:

```js
    // ── GET /fa-roster?team=LAD&min=70&max=84 ─────────────────
    // Returns all Live Series players for a team within OVR range,
    // with full attribute fields for use in the FA draft room.
    if (path === '/fa-roster') {
      const teamAbbr = (url.searchParams.get('team') || '').toUpperCase();
      const min      = parseInt(url.searchParams.get('min') || '0', 10);
      const max      = parseInt(url.searchParams.get('max') || '99', 10);
      if (!teamAbbr) return json({ error: 'team parameter required' }, 400);

      const apiTeam = TEAM_MAP[teamAbbr] || teamAbbr;

      try {
        // Fetch multiple pages to get all players in range (up to 3 pages / 75 cards)
        const pages = [1, 2, 3];
        const results = await Promise.all(pages.map(p =>
          fetch(
            `${MLB_API}/listings.json?type=mlb_card&series_id=${LIVE_SERIES}&team=${apiTeam}&sort=rank&order=desc&page=${p}`,
            { headers: { 'Accept': 'application/json', 'User-Agent': 'DNA-League-App/1.0' },
              cf: { cacheTtl: 3600, cacheEverything: true } }
          ).then(r => r.ok ? r.json() : { listings: [] }).catch(() => ({ listings: [] }))
        ));

        const allListings = results.flatMap(r => r.listings || []);

        const players = allListings
          .filter(l => l.item && l.item.ovr >= min && l.item.ovr <= max && l.item.name)
          .map(l => {
            const i = l.item;
            const isPitcher = ['SP','RP','CP'].includes(i.display_position);
            const base = {
              name:     i.name,
              pos:      i.display_position || '—',
              overall:  i.ovr,
              series:   i.series || 'Live Series',
              rarity:   i.rarity || '',
              bats:     i.bat_hand || '',
              throws:   i.throw_hand || '',
              quirks:   (i.quirks || []).map(q => q.name || q).filter(Boolean),
            };
            if (isPitcher) {
              return {
                ...base,
                stamina:         i.stamina        || 0,
                pitching_clutch: i.pitching_clutch || 0,
                hits_per_bf:     i.hits_per_bf     || 0,
                k_per_bf:        i.k_per_bf        || 0,
                bb_per_bf:       i.bb_per_bf       || 0,
                hr_per_bf:       i.hr_per_bf       || 0,
                velocity:        i.pitch_velocity  || 0,
                control:         i.pitch_control   || 0,
                break_rating:    i.pitch_break     || 0,
                pitch_arsenal: (i.pitching_attributes || []).map(p => ({
                  name:  p.name  || '',
                  speed: p.speed || 0,
                  break: p.break || 0,
                })).filter(p => p.name),
              };
            } else {
              return {
                ...base,
                contact_left:  i.contact_left  || 0,
                contact_right: i.contact_right || 0,
                power_left:    i.power_left    || 0,
                power_right:   i.power_right   || 0,
                plate_vision:  i.plate_vision  || 0,
                plate_disc:    i.plate_discipline || 0,
                clutch:        i.batting_clutch   || 0,
                speed:         i.speed            || 0,
                stealing:      i.baserunning_ability || 0,
                fielding:      i.fielding_ability  || 0,
                arm_strength:  i.arm_strength      || 0,
                arm_accuracy:  i.arm_accuracy      || 0,
              };
            }
          });

        return json({ team: teamAbbr, min, max, players, source: 'mlb26-listings' });

      } catch(e) {
        return json({ error: e.message }, 500);
      }
    }
```

Also update the 404 fallback message at the bottom to include `/fa-roster`:

```js
    return json({ error: 'Unknown endpoint. Use /roster?team=LAD, /fa-roster?team=LAD&min=70&max=84, /teams, /history?username=X&platform=Y, or /gamelog?id=X' }, 404);
```

- [ ] **Step 2: Verify route manually**

Deploy to Cloudflare Workers (dash.cloudflare.com/workers → paste updated worker.js → Save and Deploy). Then test in browser:
```
https://dna-league.josephisaacii.workers.dev/fa-roster?team=LAD&min=70&max=84
```
Expected: JSON with `{ team: "LAD", min: 70, max: 84, players: [...] }` where each player has `overall` between 70–84 and either hitter or pitcher attribute fields.

- [ ] **Step 3: Commit**

```bash
git add worker.js
git commit -m "feat: add /fa-roster Worker route with full player attributes"
```

---

## Task 2: FA Draft CSS

**Files:**
- Create: `css/fa-draft.css`

- [ ] **Step 1: Create `css/fa-draft.css`**

```css
/* ============================================================
   THE DNA LEAGUE — fa-draft.css
   Free Agent draft room styles. Shares design tokens defined
   inline in fa-draft.html (:root variables).
   ============================================================ */

/* ── LAYOUT ─────────────────────────────────────────────── */
* { margin:0; padding:0; box-sizing:border-box; }
body { background:var(--bg); color:var(--text); font-family:'Barlow',sans-serif; min-height:100vh; }

.fa-page { display:flex; flex-direction:column; min-height:100vh; }

/* ── NAV ────────────────────────────────────────────────── */
.fa-nav { position:sticky; top:0; z-index:100; background:rgba(8,11,16,0.92); backdrop-filter:blur(12px); border-bottom:1px solid var(--border); padding:0 1.5rem; height:56px; display:flex; align-items:center; gap:1rem; }
.fa-nav-logo { display:flex; align-items:center; gap:10px; text-decoration:none; color:inherit; }
.fa-nav-logo .logo-icon { width:36px; height:36px; background:linear-gradient(135deg,var(--chrome2),var(--chrome1)); clip-path:polygon(50% 0%,100% 25%,100% 75%,50% 100%,0% 75%,0% 25%); display:flex; align-items:center; justify-content:center; font-family:'Bebas Neue'; font-size:15px; color:var(--bg); flex-shrink:0; }
.fa-nav-title { font-family:'Barlow Condensed'; font-size:15px; letter-spacing:2px; text-transform:uppercase; color:var(--text2); }
.fa-nav-status { font-family:'Barlow Condensed'; font-size:11px; letter-spacing:2px; text-transform:uppercase; border-radius:4px; padding:3px 8px; }
.fa-nav-status.status-setup    { background:rgba(168,189,212,0.1); color:var(--gold); }
.fa-nav-status.status-active   { background:rgba(46,204,113,0.15); color:var(--green); }
.fa-nav-status.status-paused   { background:rgba(245,197,66,0.15); color:#f5c542; }
.fa-nav-status.status-complete { background:rgba(168,189,212,0.1); color:var(--text2); }

/* ── SETUP SCREEN ───────────────────────────────────────── */
.fa-setup { max-width:640px; margin:0 auto; padding:2rem 1.5rem; }
.fa-setup h1 { font-family:'Bebas Neue'; font-size:44px; letter-spacing:3px; line-height:1; }
.setup-card { background:var(--surface2); border-radius:12px; border:1px solid rgba(255,255,255,0.06); margin-bottom:1.25rem; overflow:hidden; }
.setup-card-header { padding:0.875rem 1.25rem; border-bottom:1px solid rgba(255,255,255,0.06); font-family:'Barlow Condensed'; font-size:11px; letter-spacing:3px; text-transform:uppercase; color:var(--text2); }
.setup-card-body { padding:1.25rem; }

/* Tier buttons */
.tier-btn { background:var(--surface3); border:1px solid rgba(255,255,255,0.1); border-radius:8px; padding:8px 14px; font-family:'Barlow Condensed'; font-size:13px; cursor:pointer; color:var(--text2); transition:border-color 0.15s,color 0.15s; display:flex; flex-direction:column; align-items:center; gap:2px; min-width:72px; }
.tier-btn .tier-range { font-size:10px; opacity:0.7; }
.tier-btn.common.active   { border-color:rgba(168,189,212,0.6); color:var(--text); }
.tier-btn.bronze.active   { border-color:#cd7f32; color:#cd7f32; background:rgba(205,127,50,0.08); }
.tier-btn.silver.active   { border-color:#c0c0c0; color:#c0c0c0; background:rgba(192,192,192,0.08); }
.tier-btn.gold.active     { border-color:#f5c542; color:#f5c542; background:rgba(245,197,66,0.08); }
.tier-btn.diamond.active  { border-color:#b9d4ff; color:#b9d4ff; background:rgba(185,212,255,0.08); }

/* ── ON-CLOCK HEADER ────────────────────────────────────── */
.fa-on-clock { background:var(--surface2); border-bottom:1px solid var(--border); padding:0.75rem 1.25rem; display:flex; align-items:center; gap:1rem; flex-wrap:wrap; }
.fa-on-clock-label { font-family:'Barlow Condensed'; font-size:11px; letter-spacing:3px; text-transform:uppercase; color:var(--text2); }
.fa-on-clock-name { font-family:'Barlow Condensed'; font-size:20px; font-weight:700; color:var(--text); letter-spacing:1px; }
.fa-on-clock-name.your-turn { color:var(--green); }
.fa-timer { font-family:'Bebas Neue'; font-size:28px; letter-spacing:2px; margin-left:auto; }
.fa-timer.urgent { color:var(--red); }
.fa-timer.warning { color:#f5c542; }
.fa-timer.ok { color:var(--green); }

/* Commissioner controls */
.fa-controls { display:flex; gap:8px; flex-wrap:wrap; }
.fa-controls .btn { font-size:12px; padding:4px 12px; }

/* ── TABS ───────────────────────────────────────────────── */
.fa-tabs { display:flex; border-bottom:1px solid var(--border); background:var(--surface); position:sticky; top:56px; z-index:90; }
.fa-tab { font-family:'Barlow Condensed'; font-size:13px; letter-spacing:2px; text-transform:uppercase; padding:12px 20px; cursor:pointer; border-bottom:2px solid transparent; color:var(--text2); background:none; border-top:none; border-left:none; border-right:none; transition:color 0.15s,border-color 0.15s; }
.fa-tab.active { color:var(--gold2); border-bottom-color:var(--gold2); }
.fa-tab-content { display:none; }
.fa-tab-content.active { display:block; }

/* ── PLAYER LIST ────────────────────────────────────────── */
.fa-player-list { padding:1rem; max-width:800px; margin:0 auto; }
.fa-filter-bar { display:flex; gap:8px; flex-wrap:wrap; margin-bottom:1rem; align-items:center; }
.fa-filter-btn { background:var(--surface3); border:1px solid rgba(255,255,255,0.08); border-radius:20px; padding:4px 12px; font-size:12px; cursor:pointer; color:var(--text2); }
.fa-filter-btn.active { background:rgba(168,189,212,0.12); border-color:rgba(168,189,212,0.4); color:var(--text); }

.fa-player-row { background:var(--surface2); border-radius:8px; margin-bottom:6px; overflow:hidden; border:1px solid rgba(255,255,255,0.04); }
.fa-player-row-header { display:flex; align-items:center; gap:10px; padding:10px 14px; cursor:pointer; }
.fa-player-row-header:hover { background:rgba(255,255,255,0.02); }
.fa-ovr-badge { width:38px; height:38px; border-radius:6px; display:flex; align-items:center; justify-content:center; font-weight:700; font-size:16px; flex-shrink:0; border-width:2px; border-style:solid; }
.fa-ovr-badge.common  { background:rgba(168,189,212,0.05); border-color:rgba(168,189,212,0.3); color:var(--text); }
.fa-ovr-badge.bronze  { background:rgba(205,127,50,0.08);  border-color:#cd7f32; color:#cd7f32; }
.fa-ovr-badge.silver  { background:rgba(192,192,192,0.08);  border-color:#c0c0c0; color:#c0c0c0; }
.fa-ovr-badge.gold    { background:rgba(245,197,66,0.08);   border-color:#f5c542; color:#f5c542; }
.fa-ovr-badge.diamond { background:rgba(185,212,255,0.08);  border-color:#b9d4ff; color:#b9d4ff; }
.fa-player-name { font-weight:600; font-size:14px; }
.fa-player-meta { font-size:11px; color:var(--text2); margin-top:2px; }
.fa-expand-btn { margin-left:auto; background:rgba(168,189,212,0.08); border:1px solid rgba(168,189,212,0.2); border-radius:6px; padding:4px 10px; font-size:12px; color:var(--text2); cursor:pointer; white-space:nowrap; flex-shrink:0; }

.fa-player-detail { border-top:1px solid rgba(255,255,255,0.06); padding:12px 14px; }
.fa-attr-section { margin-bottom:10px; }
.fa-attr-label { font-family:'Barlow Condensed'; font-size:10px; letter-spacing:2px; text-transform:uppercase; color:var(--text2); margin-bottom:6px; }
.fa-attr-grid { display:grid; grid-template-columns:1fr 1fr; gap:3px 20px; }
.fa-attr-row { display:flex; justify-content:space-between; font-size:12px; }
.fa-attr-name { color:var(--text2); }
.fa-attr-val { font-weight:600; }
.fa-attr-val.high   { color:var(--green); }
.fa-attr-val.mid    { color:#f5c542; }
.fa-attr-val.low    { color:#cd7f32; }
.fa-attr-val.vlow   { color:var(--red); }

.fa-quirks { display:flex; gap:6px; flex-wrap:wrap; }
.fa-quirk { background:rgba(46,204,113,0.1); border:1px solid rgba(46,204,113,0.2); color:var(--green); border-radius:4px; padding:2px 8px; font-size:11px; }

.fa-pitch-row { display:flex; justify-content:space-between; align-items:center; font-size:12px; padding:3px 0; border-bottom:1px solid rgba(255,255,255,0.04); }
.fa-pitch-row:last-child { border-bottom:none; }
.fa-pitch-name { color:var(--text); }
.fa-pitch-stats { display:flex; gap:12px; color:var(--text2); font-size:11px; }
.fa-pitch-stats span { color:var(--text); font-weight:600; }

.fa-select-btn { display:block; width:100%; margin-top:12px; background:linear-gradient(135deg,#c8daea,#8fafc8); color:var(--bg); border:none; border-radius:6px; padding:8px; font-family:'Barlow Condensed'; font-size:15px; font-weight:700; letter-spacing:1px; cursor:pointer; }
.fa-select-btn:disabled { opacity:0.35; cursor:not-allowed; background:var(--surface3); color:var(--text2); }

/* ── DRAFT BOARD TAB ────────────────────────────────────── */
.fa-board { padding:1rem; max-width:800px; margin:0 auto; }
.fa-board-round { margin-bottom:1.5rem; }
.fa-board-round-label { font-family:'Barlow Condensed'; font-size:11px; letter-spacing:3px; text-transform:uppercase; color:var(--text2); margin-bottom:8px; }
.fa-board-pick { display:flex; align-items:center; gap:10px; padding:8px 12px; border-radius:6px; margin-bottom:4px; background:var(--surface2); border:1px solid rgba(255,255,255,0.04); font-size:13px; }
.fa-board-pick.on-clock { border-color:var(--green); background:rgba(46,204,113,0.06); }
.fa-board-pick.done { opacity:0.7; }
.fa-board-pick-num { font-family:'Barlow Condensed'; color:var(--text2); width:28px; flex-shrink:0; }
.fa-board-pick-member { flex:1; }
.fa-board-pick-result { font-family:'Barlow Condensed'; font-size:13px; color:var(--gold2); }

/* ── MY PICKS TAB ───────────────────────────────────────── */
.fa-my-picks { padding:1rem; max-width:800px; margin:0 auto; }

/* ── EXPORT ─────────────────────────────────────────────── */
.fa-export { padding:1rem; max-width:800px; margin:0 auto; }
.fa-export-btns { display:flex; gap:10px; flex-wrap:wrap; margin-bottom:1.5rem; }

/* ── RECAP ──────────────────────────────────────────────── */
.fa-recap { padding:2rem 1.5rem; max-width:640px; margin:0 auto; text-align:center; }
.fa-recap h2 { font-family:'Bebas Neue'; font-size:44px; letter-spacing:3px; margin-bottom:0.5rem; }

/* ── TOAST ──────────────────────────────────────────────── */
#fa-toast { position:fixed; bottom:24px; left:50%; transform:translateX(-50%); background:var(--surface3); border:1px solid var(--border); border-radius:8px; padding:10px 20px; font-size:14px; z-index:999; pointer-events:none; opacity:0; transition:opacity 0.2s; white-space:nowrap; }
#fa-toast.show { opacity:1; }

/* ── RESPONSIVE ─────────────────────────────────────────── */
@media (max-width:480px) {
  .fa-setup { padding:1rem; }
  .fa-on-clock { flex-wrap:wrap; }
  .fa-timer { margin-left:0; width:100%; }
  .fa-attr-grid { grid-template-columns:1fr; }
}
```

- [ ] **Step 2: Commit**

```bash
git add css/fa-draft.css
git commit -m "feat: add fa-draft.css styles"
```

---

## Task 3: FA Draft Engine — `js/fa-draft-room.js`

**Files:**
- Create: `js/fa-draft-room.js`

This module mirrors `js/draft-room.js` in structure but operates on player picks rather than team picks.

- [ ] **Step 1: Create `js/fa-draft-room.js`**

```js
// ============================================================
// THE DNA LEAGUE — fa-draft-room.js
// Free Agent draft engine.
// Handles: config, snake order, player pool, picks, timer,
// Supabase Realtime sync, export generation.
// ============================================================

const FADraftRoom = (() => {

  // ── STATE ─────────────────────────────────────────────────
  let _db              = null;
  let _member          = null;
  let _draft           = null;   // full draft object
  let _realtimeChannel = null;
  let _playerPool      = [];     // all available players (loaded from Worker)
  let _pickedPlayerIds = new Set(); // track by "name|team" key

  let _timer        = null;
  let _timerSeconds = 0;
  let _timerTotal   = 120;

  let _isPaused  = false;
  let _isComplete = false;

  // ── INIT ──────────────────────────────────────────────────
  function init(db, member) {
    _db     = db;
    _member = member;
  }

  // ── DRAFT OBJECT SHAPE ────────────────────────────────────
  // _draft = {
  //   id, seasonId, name, status, timerSeconds,
  //   settings: { ratingMin, ratingMax, rounds },
  //   slots: [{ pickNumber, memberId, memberName, round,
  //             pickedPlayerName, pickedPlayerRating,
  //             pickedPlayerPos, pickedFromTeam,
  //             pickedAt, skipped, _dbId }],
  // }

  // ── LOAD FROM DB ──────────────────────────────────────────
  async function loadDraftFromDB(draftId) {
    const draftRes = await _db.from('drafts').select('*').eq('id', draftId).single();
    if (draftRes.error) throw new Error(draftRes.error.message);

    const slotsRes = await _db.from('draft_slots')
      .select('id, pick_number, member_id, skipped, league_members(display_name)')
      .eq('draft_id', draftId)
      .order('pick_number', { ascending: true });
    if (slotsRes.error) throw new Error(slotsRes.error.message);

    const picksRes = await _db.from('draft_picks')
      .select('id, slot_id, player_name, player_rating, mlb_team_id, picked_at')
      .eq('draft_id', draftId);
    if (picksRes.error) throw new Error(picksRes.error.message);

    const picksBySlot = {};
    (picksRes.data || []).forEach(p => { picksBySlot[p.slot_id] = p; });

    const d = draftRes.data;
    const settings = d.settings || {};

    const slots = (slotsRes.data || []).map(s => {
      const pick = picksBySlot[s.id];
      return {
        _dbId:             s.id,
        pickNumber:        s.pick_number,
        memberId:          s.member_id,
        memberName:        s.league_members?.display_name || 'Unknown',
        round:             Math.ceil(s.pick_number / _countMembers(slotsRes.data, d.settings?.rounds || 1)),
        skipped:           s.skipped || false,
        pickedPlayerName:  pick?.player_name   || null,
        pickedPlayerRating:pick?.player_rating || null,
        pickedPlayerPos:   null, // reconstructed from player pool on load
        pickedFromTeam:    pick ? _mlbTeamIdToAbbr(pick.mlb_team_id) : null,
        pickedAt:          pick?.picked_at || null,
      };
    });

    // Rebuild picked set
    _pickedPlayerIds.clear();
    slots.filter(s => s.pickedPlayerName && s.pickedFromTeam).forEach(s => {
      _pickedPlayerIds.add(`${s.pickedPlayerName}|${s.pickedFromTeam}`);
    });

    return {
      id:           d.id,
      seasonId:     d.season_id,
      name:         d.name || 'FA Draft',
      status:       d.status,
      timerSeconds: d.timer_seconds || 0,
      settings: {
        ratingMin: settings.ratingMin ?? 70,
        ratingMax: settings.ratingMax ?? 79,
        rounds:    settings.rounds    ?? 1,
      },
      slots,
    };
  }

  // Helper: count members from slots given rounds
  function _countMembers(slots, rounds) {
    return Math.round(slots.length / rounds) || slots.length;
  }

  // Will be set after mlbTeamsLookup is available
  let _mlbTeamsLookup = [];
  function setTeamsLookup(lookup) { _mlbTeamsLookup = lookup; }
  function _mlbTeamIdToAbbr(id) {
    if (!id) return '';
    const t = _mlbTeamsLookup.find(t => t.id === id);
    return t ? t.abbreviation : '';
  }
  function _mlbTeamAbbrToId(abbr) {
    const t = _mlbTeamsLookup.find(t => t.abbreviation === abbr);
    return t ? t.id : null;
  }

  function loadDraft(draft) {
    _draft        = draft;
    _timerTotal   = draft.timerSeconds || 0;
    _timerSeconds = _timerTotal;
    _isPaused     = draft.status === 'paused';
    _isComplete   = ['completed'].includes(draft.status);
  }

  // ── PLAYER POOL ───────────────────────────────────────────
  async function loadPlayerPool(teamAbbrs, ratingMin, ratingMax, workerUrl) {
    _playerPool = [];
    const results = await Promise.all(
      teamAbbrs.map(abbr =>
        fetch(`${workerUrl}/fa-roster?team=${abbr}&min=${ratingMin}&max=${ratingMax}`)
          .then(r => r.ok ? r.json() : { players: [] })
          .catch(() => ({ players: [] }))
      )
    );
    results.forEach((res, i) => {
      const abbr = teamAbbrs[i];
      (res.players || []).forEach(p => {
        _playerPool.push({ ...p, fromTeam: abbr });
      });
    });
    // Sort: highest overall first
    _playerPool.sort((a, b) => b.overall - a.overall);
    return _playerPool;
  }

  function getAvailablePlayers(posFilter) {
    return _playerPool.filter(p => {
      if (_pickedPlayerIds.has(`${p.name}|${p.fromTeam}`)) return false;
      if (posFilter && posFilter !== 'ALL') {
        const group = _posGroup(p.pos);
        if (group !== posFilter) return false;
      }
      return true;
    });
  }

  function _posGroup(pos) {
    if (['SP','RP','CP'].includes(pos)) return 'P';
    if (['C'].includes(pos))            return 'C';
    if (['1B','2B','3B','SS'].includes(pos)) return 'IF';
    if (['LF','CF','RF','DH'].includes(pos)) return 'OF';
    return 'OTHER';
  }

  // ── TIMER ─────────────────────────────────────────────────
  function startTimer() {
    if (!_timerTotal || _isPaused || _isComplete) return;
    stopTimer();
    _timerSeconds = _timerTotal;
    _renderTimer();
    _timer = setInterval(() => {
      if (_isPaused) return;
      _timerSeconds--;
      _renderTimer();
      if (_timerSeconds <= 0) {
        stopTimer();
        _onTimerExpired();
      }
    }, 1000);
  }

  function stopTimer() {
    if (_timer) { clearInterval(_timer); _timer = null; }
  }

  function resetTimer() {
    stopTimer();
    _timerSeconds = _timerTotal;
    _renderTimer();
  }

  function _renderTimer() {
    const el = document.getElementById('fa-timer-val');
    if (!el) return;
    if (!_timerTotal) { el.textContent = ''; el.className = 'fa-timer'; return; }
    const mins = Math.floor(_timerSeconds / 60);
    const secs = _timerSeconds % 60;
    el.textContent = `${mins}:${secs.toString().padStart(2,'0')}`;
    const pct = _timerSeconds / _timerTotal;
    el.className = 'fa-timer ' + (pct > 0.5 ? 'ok' : pct > 0.25 ? 'warning' : 'urgent');
  }

  async function _onTimerExpired() {
    if (!_draft) return;
    const cur = _getCurrentPick();
    if (!cur) return;
    faToast(`⏰ Time expired for ${cur.memberName} — skipping`);
    cur.skipped = true;
    await _saveSkipToDB(cur);
    _broadcast({ type: 'skip', pickNumber: cur.pickNumber });
    _advancePick();
    if (typeof renderDraftBoard === 'function') renderDraftBoard();
  }

  // ── PICK LOGIC ────────────────────────────────────────────
  function _getCurrentPick() {
    if (!_draft?.slots) return null;
    return _draft.slots.find(s => !s.pickedPlayerName && !s.skipped) || null;
  }

  function _getNextPick() {
    if (!_draft?.slots) return null;
    const active = _draft.slots.filter(s => !s.pickedPlayerName && !s.skipped);
    return active[1] || null;
  }

  function canPick(memberId) {
    const cur = _getCurrentPick();
    if (!cur) return false;
    if (cur.memberId === memberId) return true;
    if (_isAdminMember()) return true;
    return false;
  }

  function _isAdminMember() {
    return _member && ['admin','commissioner'].includes(_member.role);
  }

  async function makePick(player) {
    // player = { name, overall, pos, fromTeam, ...attrs }
    const cur = _getCurrentPick();
    if (!cur) { faToast('No active pick slot'); return; }
    if (cur.memberId !== _member?.id && !_isAdminMember()) {
      faToast("It's not your turn"); return;
    }
    if (cur.memberId !== _member?.id && _isAdminMember()) {
      if (!confirm(`Pick ${player.name} on behalf of ${cur.memberName}?`)) return;
    }
    const key = `${player.name}|${player.fromTeam}`;
    if (_pickedPlayerIds.has(key)) { faToast('Player already picked'); return; }

    cur.pickedPlayerName   = player.name;
    cur.pickedPlayerRating = player.overall;
    cur.pickedPlayerPos    = player.pos;
    cur.pickedFromTeam     = player.fromTeam;
    cur.pickedAt           = new Date().toISOString();
    _pickedPlayerIds.add(key);

    _broadcast({ type: 'pick', pickNumber: cur.pickNumber, playerName: player.name,
                 playerRating: player.overall, playerPos: player.pos, fromTeam: player.fromTeam });

    await _savePickToDB(cur, player);

    const remaining = _draft.slots.filter(s => !s.pickedPlayerName && !s.skipped);
    if (remaining.length === 0) {
      await _completeDraft();
    } else {
      _advancePick();
    }
    if (typeof renderDraftBoard === 'function') renderDraftBoard();
    if (typeof renderAvailablePlayers === 'function') renderAvailablePlayers();
  }

  function _advancePick() {
    resetTimer();
    startTimer();
    if (typeof updateOnClock === 'function') updateOnClock();
    if (typeof checkYourTurn === 'function') checkYourTurn();
    const next = _getCurrentPick();
    if (next) faToast(`Now on the clock: ${next.memberName}`);
  }

  async function _completeDraft() {
    stopTimer();
    _isComplete = true;
    _draft.status = 'completed';
    _draft.completedAt = new Date().toISOString();
    await _saveStatusToDB('completed', _draft.completedAt);
    _broadcast({ type: 'complete' });
    if (typeof updateOnClock === 'function') updateOnClock();
    if (typeof showRecap === 'function') showRecap();
    faToast('FA Draft complete! 🎉');
  }

  async function undoLastPick() {
    if (!_isAdminMember()) return;
    const lastPicked = [..._draft.slots].reverse().find(s => s.pickedPlayerName);
    if (!lastPicked) { faToast('No picks to undo'); return; }
    if (!confirm(`Undo ${lastPicked.memberName}'s pick of ${lastPicked.pickedPlayerName}?`)) return;
    const key = `${lastPicked.pickedPlayerName}|${lastPicked.pickedFromTeam}`;
    _pickedPlayerIds.delete(key);
    lastPicked.pickedPlayerName   = null;
    lastPicked.pickedPlayerRating = null;
    lastPicked.pickedPlayerPos    = null;
    lastPicked.pickedFromTeam     = null;
    lastPicked.pickedAt           = null;
    _broadcast({ type: 'undo', pickNumber: lastPicked.pickNumber });
    await _deletePickFromDB(lastPicked);
    if (typeof renderDraftBoard === 'function') renderDraftBoard();
    if (typeof renderAvailablePlayers === 'function') renderAvailablePlayers();
    resetTimer(); startTimer();
    faToast('Pick undone');
  }

  async function pauseDraft() {
    if (!_isAdminMember()) return;
    _isPaused = true;
    _draft.status = 'paused';
    stopTimer();
    await _saveStatusToDB('paused');
    _broadcast({ type: 'pause' });
    faToast('Draft paused');
    if (typeof updatePauseBtn === 'function') updatePauseBtn(true);
  }

  async function resumeDraft() {
    if (!_isAdminMember()) return;
    _isPaused = false;
    _draft.status = 'active';
    await _saveStatusToDB('active');
    _broadcast({ type: 'resume' });
    startTimer();
    faToast('Draft resumed');
    if (typeof updatePauseBtn === 'function') updatePauseBtn(false);
  }

  async function manualSkipCurrent() {
    if (!_isAdminMember()) return;
    const cur = _getCurrentPick();
    if (!cur) return;
    cur.skipped = true;
    await _saveSkipToDB(cur);
    _broadcast({ type: 'skip', pickNumber: cur.pickNumber });
    const remaining = _draft.slots.filter(s => !s.pickedPlayerName && !s.skipped);
    if (remaining.length === 0) {
      await _completeDraft();
    } else {
      _advancePick();
    }
    if (typeof renderDraftBoard === 'function') renderDraftBoard();
    faToast(`Skipped ${cur.memberName}`);
  }

  async function endDraftEarly() {
    if (!_isAdminMember()) return;
    if (!confirm('End the FA draft now? Remaining picks will be forfeited.')) return;
    await _completeDraft();
  }

  // ── EXPORT ────────────────────────────────────────────────
  // Requires _playerPool to be loaded so we can look up full rosters
  // for trade return player selection.

  function getExportData() {
    if (!_draft) return [];
    return _draft.slots
      .filter(s => s.pickedPlayerName)
      .map(s => {
        const tradeReturn = _findTradeReturn(s);
        return {
          round:               s.round,
          pick:                s.pickNumber,
          playerName:          s.pickedPlayerName,
          playerOvr:           s.pickedPlayerRating,
          playerPos:           s.pickedPlayerPos || '',
          originalTeam:        s.pickedFromTeam  || '',
          draftedBy:           s.memberName,
          tradeReturnName:     tradeReturn?.name    || '—',
          tradeReturnOvr:      tradeReturn?.overall || '—',
          tradeReturnPos:      tradeReturn?.pos     || '—',
        };
      });
  }

  function _findTradeReturn(slot) {
    // Get all players on the receiving team from the pool (full pool, not filtered by range)
    // We use whatever was loaded. In practice commissioner runs export after draft so pool is loaded.
    const teamPlayers = _playerPool.filter(p => p.fromTeam === slot.pickedFromTeam);
    if (!teamPlayers.length) return null;

    const draftedPos = slot.pickedPlayerPos || '';
    const group = _posGroup(draftedPos);
    const isPitcher = group === 'P';

    // Sort ascending by overall
    const byOvr = [...teamPlayers].sort((a, b) => a.overall - b.overall);

    // Matching group
    const sameGroup = byOvr.filter(p => _posGroup(p.pos) === group);
    if (sameGroup.length) {
      const candidate = sameGroup[0];
      // Fallback if candidate is within 5 OVR of drafted player (too valuable)
      if (Math.abs(candidate.overall - slot.pickedPlayerRating) <= 5) {
        return byOvr[0]; // absolute lowest
      }
      return candidate;
    }

    // Fallback: same broader group (IF/OF share fallback)
    if (!isPitcher) {
      const posPlayers = byOvr.filter(p => _posGroup(p.pos) !== 'P');
      if (posPlayers.length) return posPlayers[0];
    }

    return byOvr[0]; // absolute lowest
  }

  function downloadCSV() {
    const rows = getExportData();
    if (!rows.length) { faToast('No picks to export'); return; }
    const headers = ['Round','Pick','Player','OVR','Pos','Original Team','Drafted By','Trade Return','Return OVR','Return Pos'];
    const lines = [headers.join(',')];
    rows.forEach(r => {
      lines.push([
        r.round, r.pick,
        `"${r.playerName}"`, r.playerOvr, r.playerPos,
        r.originalTeam, `"${r.draftedBy}"`,
        `"${r.tradeReturnName}"`, r.tradeReturnOvr, r.tradeReturnPos,
      ].join(','));
    });
    const blob = new Blob([lines.join('\n')], { type: 'text/csv' });
    const a = document.createElement('a');
    a.href = URL.createObjectURL(blob);
    a.download = `fa-draft-${_draft.seasonId || 'results'}.csv`;
    a.click();
  }

  function openPrintChecklist() {
    const rows = getExportData();
    if (!rows.length) { faToast('No picks to export'); return; }

    // Group by original team
    const byTeam = {};
    rows.forEach(r => {
      if (!byTeam[r.originalTeam]) byTeam[r.originalTeam] = [];
      byTeam[r.originalTeam].push(r);
    });

    const tierColor = ovr => ovr >= 85 ? '#b9d4ff' : ovr >= 80 ? '#f5c542' : ovr >= 75 ? '#c0c0c0' : ovr >= 70 ? '#cd7f32' : '#a8bdd4';

    const teamSections = Object.keys(byTeam).sort().map(team => {
      const picks = byTeam[team];
      const tradeRows = picks.map((r, idx) => `
        <div class="trade-row" id="tr-${team}-${idx}">
          <input type="checkbox" onchange="document.getElementById('tr-${team}-${idx}').classList.toggle('done',this.checked);updateProgress()">
          <div class="trade-info">
            <div class="trade-player">
              <strong>${r.playerName}</strong>
              <span class="ovr-badge" style="color:${tierColor(r.playerOvr)};border-color:${tierColor(r.playerOvr)}">${r.playerOvr}</span>
              <span class="pos">${r.playerPos}</span>
              <span class="pick-num">Rd ${r.round}, Pick ${r.pick}</span>
            </div>
            <div class="trade-details">
              Drafted by: <strong>${r.draftedBy}</strong>
              &nbsp;·&nbsp;
              Trade return: <strong style="color:#d94040">${r.tradeReturnName}</strong>
              <span class="ovr-badge" style="color:#d94040;border-color:#d94040">${r.tradeReturnOvr}</span>
              <span class="pos">${r.tradeReturnPos}</span>
            </div>
            <div class="trade-hint">Rejoin as ${team} → trade ${r.playerName} to ${r.draftedBy}'s team</div>
          </div>
        </div>`).join('');
      return `<div class="team-section"><div class="team-header">${team} &mdash; ${picks.length} trade${picks.length !== 1 ? 's' : ''}</div>${tradeRows}</div>`;
    }).join('');

    const total = rows.length;
    const html = `<!DOCTYPE html><html><head><meta charset="UTF-8">
<title>FA Draft Trade Checklist</title>
<style>
  body{font-family:Arial,sans-serif;background:#fff;color:#111;max-width:800px;margin:0 auto;padding:20px;}
  h1{font-size:22px;margin-bottom:4px;}
  .subtitle{color:#555;font-size:13px;margin-bottom:16px;}
  .instructions{background:#f0f4ff;border:1px solid #c0d0ff;border-radius:6px;padding:10px 14px;font-size:12px;margin-bottom:20px;}
  .team-section{margin-bottom:24px;}
  .team-header{font-size:14px;font-weight:700;border-bottom:2px solid #333;padding-bottom:4px;margin-bottom:10px;}
  .trade-row{display:flex;align-items:flex-start;gap:10px;padding:8px 0;border-bottom:1px solid #eee;}
  .trade-row.done{opacity:0.4;}
  .trade-row input{margin-top:4px;width:16px;height:16px;flex-shrink:0;}
  .trade-player{display:flex;align-items:center;gap:6px;flex-wrap:wrap;margin-bottom:3px;}
  .ovr-badge{border:1px solid;border-radius:3px;padding:1px 5px;font-size:11px;font-weight:700;}
  .pos{color:#555;font-size:12px;}
  .pick-num{color:#888;font-size:11px;}
  .trade-details{font-size:12px;color:#555;margin-bottom:2px;}
  .trade-hint{font-size:11px;color:#888;font-style:italic;}
  .progress{background:#f5f5f5;border:1px solid #ddd;border-radius:6px;padding:8px 14px;display:flex;justify-content:space-between;font-size:13px;margin-top:20px;}
  @media print{.progress{position:fixed;bottom:0;left:0;right:0;border-radius:0;}}
</style>
</head><body>
<h1>FA Draft Trade Checklist</h1>
<div class="subtitle">Season Draft — ${total} trades to execute</div>
<div class="instructions"><strong>How to execute:</strong> For each trade, leave the league → rejoin as the <em>original team</em> → trade the drafted player to the new owner's team (receive the listed return player back).</div>
${teamSections}
<div class="progress"><span>Progress: <strong id="prog">0</strong> / ${total} trades</span></div>
<script>function updateProgress(){document.getElementById('prog').textContent=document.querySelectorAll('.trade-row.done').length;}<\/script>
</body></html>`;

    const w = window.open('', '_blank');
    w.document.write(html);
    w.document.close();
  }

  // ── PERSISTENCE ───────────────────────────────────────────
  async function _savePickToDB(slot, player) {
    const mlbTeamId = _mlbTeamAbbrToId(player.fromTeam);
    const res = await _db.from('draft_picks').insert({
      draft_id:     _draft.id,
      slot_id:      slot._dbId,
      member_id:    slot.memberId,
      pick_number:  slot.pickNumber,
      player_name:  player.name,
      player_rating:player.overall,
      mlb_team_id:  mlbTeamId || null,
      picked_at:    slot.pickedAt,
    });
    if (res.error) console.error('_savePickToDB:', res.error.message);
  }

  async function _saveSkipToDB(slot) {
    if (!slot._dbId) return;
    const res = await _db.from('draft_slots').update({ skipped: true }).eq('id', slot._dbId);
    if (res.error) console.error('_saveSkipToDB:', res.error.message);
  }

  async function _saveStatusToDB(status, completedAt) {
    const update = { status, paused: status === 'paused' };
    if (completedAt) update.completed_at = completedAt;
    const res = await _db.from('drafts').update(update).eq('id', _draft.id);
    if (res.error) console.error('_saveStatusToDB:', res.error.message);
  }

  async function _deletePickFromDB(slot) {
    const res = await _db.from('draft_picks').delete().eq('slot_id', slot._dbId);
    if (res.error) console.error('_deletePickFromDB:', res.error.message);
  }

  // ── REALTIME ──────────────────────────────────────────────
  function _broadcast(event) {
    if (!_realtimeChannel) return;
    _realtimeChannel.send({
      type: 'broadcast', event: 'fa_draft_event',
      payload: { ...event, draftId: _draft?.id, ts: Date.now() },
    }).catch(e => console.warn('broadcast error:', e));
  }

  function subscribeRealtime(draftId) {
    if (!_db) return;
    _realtimeChannel = _db.channel(`fa_draft:${draftId}`)
      .on('broadcast', { event: 'fa_draft_event' }, ({ payload }) => {
        _handleRemoteEvent(payload);
      })
      .subscribe();
  }

  function unsubscribeRealtime() {
    if (_realtimeChannel) { _db.removeChannel(_realtimeChannel); _realtimeChannel = null; }
  }

  function _handleRemoteEvent(payload) {
    if (!_draft || payload.ts <= (_draft._lastEventTs || 0)) return;
    _draft._lastEventTs = payload.ts;

    if (payload.type === 'pick') {
      const slot = _draft.slots.find(s => s.pickNumber === payload.pickNumber);
      if (slot && !slot.pickedPlayerName) {
        slot.pickedPlayerName   = payload.playerName;
        slot.pickedPlayerRating = payload.playerRating;
        slot.pickedPlayerPos    = payload.playerPos;
        slot.pickedFromTeam     = payload.fromTeam;
        slot.pickedAt           = new Date().toISOString();
        _pickedPlayerIds.add(`${payload.playerName}|${payload.fromTeam}`);
        _advancePick();
        if (typeof renderDraftBoard === 'function') renderDraftBoard();
        if (typeof renderAvailablePlayers === 'function') renderAvailablePlayers();
      }
    } else if (payload.type === 'skip') {
      const slot = _draft.slots.find(s => s.pickNumber === payload.pickNumber);
      if (slot) { slot.skipped = true; _advancePick(); if (typeof renderDraftBoard === 'function') renderDraftBoard(); }
    } else if (payload.type === 'undo') {
      const slot = _draft.slots.find(s => s.pickNumber === payload.pickNumber);
      if (slot) {
        const key = `${slot.pickedPlayerName}|${slot.pickedFromTeam}`;
        _pickedPlayerIds.delete(key);
        slot.pickedPlayerName = null; slot.pickedPlayerRating = null;
        slot.pickedPlayerPos = null; slot.pickedFromTeam = null; slot.pickedAt = null;
        if (typeof renderDraftBoard === 'function') renderDraftBoard();
        if (typeof renderAvailablePlayers === 'function') renderAvailablePlayers();
        resetTimer(); startTimer();
      }
    } else if (payload.type === 'pause') {
      _isPaused = true; _draft.status = 'paused'; stopTimer();
      if (typeof updatePauseBtn === 'function') updatePauseBtn(true);
    } else if (payload.type === 'resume') {
      _isPaused = false; _draft.status = 'active';
      startTimer();
      if (typeof updatePauseBtn === 'function') updatePauseBtn(false);
    } else if (payload.type === 'complete') {
      _isComplete = true; _draft.status = 'completed'; stopTimer();
      if (typeof updateOnClock === 'function') updateOnClock();
      if (typeof showRecap === 'function') showRecap();
    }
  }

  // ── PUBLIC API ────────────────────────────────────────────
  return {
    init, setTeamsLookup, loadDraftFromDB, loadDraft,
    loadPlayerPool, getAvailablePlayers,
    startTimer, stopTimer, resetTimer,
    canPick, makePick, undoLastPick,
    pauseDraft, resumeDraft, manualSkipCurrent, endDraftEarly,
    getCurrentPick: _getCurrentPick,
    getNextPick: _getNextPick,
    downloadCSV, openPrintChecklist, getExportData,
    subscribeRealtime, unsubscribeRealtime,
  };
})();
```

- [ ] **Step 2: Commit**

```bash
git add js/fa-draft-room.js
git commit -m "feat: add fa-draft-room.js engine"
```

---

## Task 4: FA Draft HTML page — `fa-draft.html`

**Files:**
- Create: `fa-draft.html`

This is the standalone page. It handles auth, shows the config screen (commissioner) or the live room (all members), and wires up `FADraftRoom`.

- [ ] **Step 1: Create `fa-draft.html`**

```html
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
<meta name="apple-mobile-web-app-capable" content="yes">
<title>FA Draft — The DNA League</title>
<link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Barlow+Condensed:wght@300;400;600;700&family=Barlow:wght@300;400;500&display=swap" rel="stylesheet">
<link rel="stylesheet" href="css/fa-draft.css">
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
<script src="js/config.js"></script>
<script src="js/fa-draft-room.js"></script>
<style>
  :root {
    --bg:#0a0d12; --surface:#0f1318; --surface2:#151c24; --surface3:#1c2530;
    --gold:#a8bdd4; --gold2:#d0e4f5; --red:#d94040; --green:#2ecc71;
    --blue:#6a9ec7; --text:#dde6ef; --text2:#7a93aa;
    --border:rgba(168,189,212,0.2);
    --chrome1:#c8daea; --chrome2:#8fafc8; --chrome3:#4a7a9b;
  }
  /* Shared btn styles */
  .btn { display:inline-flex; align-items:center; gap:6px; padding:8px 18px; border-radius:8px; border:1px solid var(--border); background:transparent; color:var(--text); font-family:'Barlow Condensed'; font-size:14px; letter-spacing:1px; cursor:pointer; transition:background 0.15s,border-color 0.15s; }
  .btn:hover { background:rgba(168,189,212,0.06); }
  .btn-gold { background:linear-gradient(135deg,var(--chrome2),var(--chrome1)); color:var(--bg); border:none; font-weight:700; }
  .btn-outline { border-color:rgba(168,189,212,0.3); }
  .btn-ghost { border-color:transparent; }
  .btn-sm { font-size:12px; padding:5px 12px; }
  .form-group { display:flex; flex-direction:column; gap:6px; }
  .form-group label { font-family:'Barlow Condensed'; font-size:11px; letter-spacing:2px; text-transform:uppercase; color:var(--text2); }
  .form-input { background:var(--surface3); border:1px solid rgba(255,255,255,0.1); border-radius:6px; color:var(--text); padding:7px 12px; font-size:14px; font-family:'Barlow',sans-serif; width:100%; }
  select.form-input option { background:var(--surface3); }

  /* Screens */
  #screen-auth, #screen-setup, #screen-draft, #screen-loading { display:none; }
  #screen-auth.show, #screen-setup.show, #screen-draft.show, #screen-loading.show { display:block; }

  /* Auth screen */
  .auth-wrap { max-width:380px; margin:80px auto; padding:1.5rem; background:var(--surface2); border-radius:12px; border:1px solid var(--border); }
  .auth-wrap h2 { font-family:'Bebas Neue'; font-size:32px; letter-spacing:2px; margin-bottom:1rem; }
</style>
</head>
<body>

<div id="fa-toast"></div>

<div class="fa-page">

  <!-- NAV -->
  <nav class="fa-nav">
    <a href="index.html" class="fa-nav-logo" title="Back to League">
      <div class="logo-icon">DNA</div>
      <div style="font-family:'Barlow Condensed';font-size:13px;letter-spacing:1px;">DNA League</div>
    </a>
    <div class="fa-nav-title" id="fa-nav-title">FA Draft</div>
    <div class="fa-nav-status status-setup" id="fa-nav-status">Setup</div>
    <div style="margin-left:auto;">
      <span id="fa-nav-user" style="font-family:'Barlow Condensed';font-size:13px;color:var(--text2);letter-spacing:1px;"></span>
    </div>
  </nav>

  <!-- AUTH SCREEN -->
  <div id="screen-auth">
    <div class="auth-wrap">
      <h2>Sign In</h2>
      <div class="form-group" style="margin-bottom:1rem;">
        <label>Email</label>
        <input type="email" id="auth-email" class="form-input" placeholder="your@email.com">
      </div>
      <div class="form-group" style="margin-bottom:1.25rem;">
        <label>Password</label>
        <input type="password" id="auth-password" class="form-input" placeholder="Password">
      </div>
      <button class="btn btn-gold" style="width:100%;" onclick="signIn()">Sign In</button>
      <div id="auth-error" style="margin-top:0.75rem;font-size:13px;color:var(--red);"></div>
    </div>
  </div>

  <!-- LOADING -->
  <div id="screen-loading">
    <div style="padding:4rem;text-align:center;color:var(--text2);font-family:'Barlow Condensed';font-size:14px;letter-spacing:2px;">LOADING...</div>
  </div>

  <!-- SETUP SCREEN (commissioner only) -->
  <div id="screen-setup">
    <div class="fa-setup">
      <div style="margin-bottom:2rem;">
        <h1>FA Draft <span style="color:var(--gold2);">Setup</span></h1>
        <p style="color:var(--text2);margin-top:6px;" id="setup-subtitle">Configure the free agent draft</p>
      </div>

      <!-- Rating Range -->
      <div class="setup-card">
        <div class="setup-card-header">Player Rating Range</div>
        <div class="setup-card-body">
          <div style="display:flex;gap:8px;flex-wrap:wrap;margin-bottom:0.875rem;">
            <button class="tier-btn common"  onclick="selectTier(65,69,'common')" >Common<span class="tier-range">65–69</span></button>
            <button class="tier-btn bronze"  onclick="selectTier(70,74,'bronze')" >Bronze<span class="tier-range">70–74</span></button>
            <button class="tier-btn silver"  onclick="selectTier(75,79,'silver')" >Silver<span class="tier-range">75–79</span></button>
            <button class="tier-btn gold"    onclick="selectTier(80,84,'gold')"   >Gold<span class="tier-range">80–84</span></button>
            <button class="tier-btn diamond" onclick="selectTier(85,99,'diamond')">Diamond<span class="tier-range">85–99</span></button>
          </div>
          <div style="display:flex;gap:12px;align-items:flex-end;flex-wrap:wrap;">
            <div class="form-group">
              <label>Min Overall</label>
              <input type="number" id="setup-min" class="form-input" style="width:80px;" value="70" min="60" max="99" oninput="syncTierHighlight()">
            </div>
            <div style="padding-bottom:9px;color:var(--text2);">—</div>
            <div class="form-group">
              <label>Max Overall</label>
              <input type="number" id="setup-max" class="form-input" style="width:80px;" value="79" min="60" max="99" oninput="syncTierHighlight()">
            </div>
          </div>
        </div>
      </div>

      <!-- Rounds & Timer -->
      <div class="setup-card">
        <div class="setup-card-header">Draft Settings</div>
        <div class="setup-card-body" style="display:grid;grid-template-columns:1fr 1fr;gap:1rem;">
          <div class="form-group">
            <label>Rounds</label>
            <input type="number" id="setup-rounds" class="form-input" style="width:80px;" value="1" min="1" max="20">
            <div style="font-size:11px;color:var(--text2);">Snake order if &gt; 1</div>
          </div>
          <div class="form-group">
            <label>Pick Timer</label>
            <select id="setup-timer" class="form-input" style="width:140px;">
              <option value="0">No timer</option>
              <option value="120" selected>2 minutes</option>
              <option value="180">3 minutes</option>
              <option value="300">5 minutes</option>
            </select>
          </div>
        </div>
      </div>

      <!-- Draft Order Preview -->
      <div class="setup-card">
        <div class="setup-card-header">Draft Order <span style="font-size:10px;color:var(--text2);letter-spacing:1px;">(reversed from team draft)</span></div>
        <div class="setup-card-body">
          <div id="setup-order-preview" style="display:flex;gap:8px;flex-wrap:wrap;"></div>
        </div>
      </div>

      <button class="btn btn-gold" style="font-size:16px;padding:12px 32px;" onclick="launchFADraft()">Launch FA Draft</button>
    </div>
  </div>

  <!-- DRAFT ROOM -->
  <div id="screen-draft">

    <!-- On-Clock Header -->
    <div class="fa-on-clock" id="fa-on-clock-bar">
      <div>
        <div class="fa-on-clock-label">On the Clock</div>
        <div class="fa-on-clock-name" id="fa-on-clock-name">—</div>
      </div>
      <div id="fa-timer-val" class="fa-timer ok"></div>
      <div class="fa-controls" id="fa-controls" style="display:none;">
        <button class="btn btn-sm btn-outline" id="fa-pause-btn" onclick="togglePause()">Pause</button>
        <button class="btn btn-sm btn-outline" onclick="FADraftRoom.manualSkipCurrent()">Skip</button>
        <button class="btn btn-sm btn-outline" onclick="FADraftRoom.undoLastPick()">Undo</button>
        <button class="btn btn-sm" onclick="FADraftRoom.endDraftEarly()" style="color:var(--red);border-color:rgba(217,64,64,0.3);">End Draft</button>
      </div>
    </div>

    <!-- Tabs -->
    <div class="fa-tabs">
      <button class="fa-tab active" onclick="switchTab('players',this)">Available Players</button>
      <button class="fa-tab"        onclick="switchTab('board',this)">Draft Board</button>
      <button class="fa-tab"        onclick="switchTab('mypicks',this)">My Picks</button>
      <button class="fa-tab"        id="fa-export-tab" style="display:none;" onclick="switchTab('export',this)">Export</button>
    </div>

    <!-- Tab: Available Players -->
    <div id="tab-players" class="fa-tab-content active">
      <div class="fa-player-list">
        <div class="fa-filter-bar">
          <span style="font-size:12px;color:var(--text2);">Filter:</span>
          <button class="fa-filter-btn active" onclick="filterPlayers('ALL',this)">All</button>
          <button class="fa-filter-btn" onclick="filterPlayers('P',this)">Pitchers</button>
          <button class="fa-filter-btn" onclick="filterPlayers('C',this)">Catchers</button>
          <button class="fa-filter-btn" onclick="filterPlayers('IF',this)">Infield</button>
          <button class="fa-filter-btn" onclick="filterPlayers('OF',this)">Outfield</button>
        </div>
        <div id="fa-player-list-inner"></div>
      </div>
    </div>

    <!-- Tab: Draft Board -->
    <div id="tab-board" class="fa-tab-content">
      <div class="fa-board" id="fa-board-inner"></div>
    </div>

    <!-- Tab: My Picks -->
    <div id="tab-mypicks" class="fa-tab-content">
      <div class="fa-my-picks" id="fa-mypicks-inner"></div>
    </div>

    <!-- Tab: Export (shown after draft complete) -->
    <div id="tab-export" class="fa-tab-content">
      <div class="fa-export">
        <h2 style="font-family:'Bebas Neue';font-size:36px;letter-spacing:2px;margin-bottom:1rem;">Draft Complete</h2>
        <div class="fa-export-btns">
          <button class="btn btn-outline" onclick="FADraftRoom.downloadCSV()">⬇ Download CSV</button>
          <button class="btn btn-gold" onclick="FADraftRoom.openPrintChecklist()">🖨 Print Trade Checklist</button>
        </div>
        <div id="fa-export-summary"></div>
      </div>
    </div>

  </div><!-- end screen-draft -->

</div><!-- end fa-page -->

<script>
// ── GLOBALS ───────────────────────────────────────────────
const db = supabase.createClient(DNA_CONFIG.supabase.url, DNA_CONFIG.supabase.anonKey);
let currentUser   = null;
let currentMember = null;
let currentLeague = null;
let currentSeason = null;
let mlbTeamsLookup = [];
let activeDraftId  = null;
let currentPosFilter = 'ALL';

// ── TOAST ─────────────────────────────────────────────────
function faToast(msg, dur) {
  const el = document.getElementById('fa-toast');
  el.textContent = msg;
  el.classList.add('show');
  setTimeout(() => el.classList.remove('show'), dur || 2500);
}
window.faToast = faToast; // make accessible to FADraftRoom

// ── AUTH ──────────────────────────────────────────────────
async function signIn() {
  const email    = document.getElementById('auth-email').value.trim();
  const password = document.getElementById('auth-password').value;
  const errEl    = document.getElementById('auth-error');
  errEl.textContent = '';
  const { error } = await db.auth.signInWithPassword({ email, password });
  if (error) { errEl.textContent = error.message; }
}

async function boot() {
  showScreen('loading');
  const { data: { session } } = await db.auth.getSession();
  if (!session) { showScreen('auth'); return; }
  currentUser = session.user;
  await loadContext();

  db.auth.onAuthStateChange(async (event, session) => {
    if (event === 'SIGNED_IN' && session) {
      currentUser = session.user;
      await loadContext();
    } else if (event === 'SIGNED_OUT') {
      showScreen('auth');
    }
  });
}

async function loadContext() {
  showScreen('loading');
  // Load member + league
  const mRes = await db.from('league_members')
    .select('*, leagues(*)')
    .eq('user_id', currentUser.id)
    .maybeSingle();
  if (!mRes.data) { faToast('Member record not found'); showScreen('auth'); return; }
  currentMember = mRes.data;
  currentLeague = mRes.data.leagues;

  document.getElementById('fa-nav-user').textContent = currentMember.display_name;

  // Load mlbTeamsLookup
  const teamsRes = await db.from('mlb_teams').select('id, name, abbreviation').order('name');
  mlbTeamsLookup = teamsRes.data || [];
  FADraftRoom.init(db, currentMember);
  FADraftRoom.setTeamsLookup(mlbTeamsLookup);

  // Get season from URL param
  const params   = new URL(location.href).searchParams;
  const seasonId = params.get('season');
  if (!seasonId) { faToast('No season specified'); showScreen('auth'); return; }

  const sRes = await db.from('seasons')
    .select('id, name, status')
    .eq('id', seasonId)
    .single();
  if (sRes.error) { faToast('Season not found'); return; }
  currentSeason = sRes.data;

  // Check for existing FA draft for this season
  const dRes = await db.from('drafts')
    .select('id, status')
    .eq('season_id', seasonId)
    .eq('type', 'player')
    .maybeSingle();

  if (!dRes.data) {
    // No FA draft yet — show config to commissioner
    if (!_isAdmin()) { faToast('No FA draft created yet — ask your commissioner'); showScreen('loading'); return; }
    await showSetupScreen(seasonId);
  } else {
    activeDraftId = dRes.data.id;
    await enterDraftRoom(activeDraftId);
  }
}

function _isAdmin() {
  return currentMember && ['admin','commissioner'].includes(currentMember.role);
}

// ── SETUP SCREEN ──────────────────────────────────────────
async function showSetupScreen(seasonId) {
  // Build draft order preview from existing team draft slots (reversed)
  const teamDraftRes = await db.from('drafts')
    .select('id')
    .eq('season_id', seasonId)
    .eq('type', 'team')
    .maybeSingle();

  let orderNames = [];
  if (teamDraftRes.data) {
    const slotsRes = await db.from('draft_slots')
      .select('pick_number, member_id, league_members(display_name)')
      .eq('draft_id', teamDraftRes.data.id)
      .order('pick_number', { ascending: true });
    if (slotsRes.data) {
      // Take only unique members (first-round slots), then reverse
      const seen = new Set();
      const unique = [];
      slotsRes.data.forEach(s => {
        if (!seen.has(s.member_id)) { seen.add(s.member_id); unique.push(s); }
      });
      unique.reverse();
      orderNames = unique.map(s => s.league_members?.display_name || 'Unknown');
    }
  }

  const preview = document.getElementById('setup-order-preview');
  if (orderNames.length) {
    preview.innerHTML = orderNames.map((n, i) =>
      `<div style="background:var(--surface3);border-radius:6px;padding:4px 10px;font-size:12px;">
        <span style="color:var(--text2);">${i+1}.</span> ${escHtml(n)}
      </div>`
    ).join('');
  } else {
    preview.innerHTML = '<div style="font-size:13px;color:var(--text2);">No team draft found — order will be set when you launch.</div>';
  }

  document.getElementById('fa-nav-title').textContent = 'FA Draft — ' + currentSeason.name;
  showScreen('setup');
}

function escHtml(s) {
  return String(s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');
}

function selectTier(min, max, cls) {
  document.getElementById('setup-min').value = min;
  document.getElementById('setup-max').value = max;
  syncTierHighlight(cls);
}

function syncTierHighlight(activeCls) {
  const min = parseInt(document.getElementById('setup-min').value);
  const max = parseInt(document.getElementById('setup-max').value);
  const tiers = { common:[65,69], bronze:[70,74], silver:[75,79], gold:[80,84], diamond:[85,99] };
  document.querySelectorAll('.tier-btn').forEach(btn => {
    btn.classList.remove('active');
    const cls = [...btn.classList].find(c => tiers[c]);
    if (cls && tiers[cls][0] === min && tiers[cls][1] === max) btn.classList.add('active');
  });
}

async function launchFADraft() {
  const ratingMin   = parseInt(document.getElementById('setup-min').value) || 70;
  const ratingMax   = parseInt(document.getElementById('setup-max').value) || 79;
  const rounds      = parseInt(document.getElementById('setup-rounds').value) || 1;
  const timerSecs   = parseInt(document.getElementById('setup-timer').value) || 0;

  if (ratingMin > ratingMax) { faToast('Min must be ≤ Max'); return; }

  const params   = new URL(location.href).searchParams;
  const seasonId = params.get('season');

  // Get reversed team draft order
  const teamDraftRes = await db.from('drafts').select('id').eq('season_id', seasonId).eq('type','team').maybeSingle();
  let memberOrder = [];
  if (teamDraftRes.data) {
    const slotsRes = await db.from('draft_slots')
      .select('pick_number, member_id')
      .eq('draft_id', teamDraftRes.data.id)
      .order('pick_number', { ascending: true });
    const seen = new Set(); const unique = [];
    (slotsRes.data || []).forEach(s => { if (!seen.has(s.member_id)) { seen.add(s.member_id); unique.push(s.member_id); } });
    unique.reverse();
    memberOrder = unique;
  }

  if (!memberOrder.length) { faToast('No team draft found to derive order'); return; }

  // Create draft row
  const draftRes = await db.from('drafts').insert({
    season_id:     seasonId,
    league_id:     currentLeague.id,
    type:          'player',
    status:        'active',
    order_mode:    'weighted',
    timer_seconds: timerSecs,
    settings:      { ratingMin, ratingMax, rounds },
    name:          'FA Draft — ' + currentSeason.name,
  }).select().single();

  if (draftRes.error) { faToast('Error creating draft: ' + draftRes.error.message); return; }
  activeDraftId = draftRes.data.id;

  // Create draft_slots for all rounds (snake order encoded)
  const slots = [];
  let pickNum = 1;
  for (let round = 1; round <= rounds; round++) {
    const order = (round % 2 === 1) ? memberOrder : [...memberOrder].reverse();
    order.forEach(memberId => {
      slots.push({ draft_id: activeDraftId, pick_number: pickNum++, member_id: memberId, skipped: false });
    });
  }
  const slotsRes = await db.from('draft_slots').insert(slots);
  if (slotsRes.error) { faToast('Error creating slots: ' + slotsRes.error.message); return; }

  await enterDraftRoom(activeDraftId);
}

// ── DRAFT ROOM ─────────────────────────────────────────────
async function enterDraftRoom(draftId) {
  showScreen('loading');
  try {
    const draft = await FADraftRoom.loadDraftFromDB(draftId);
    FADraftRoom.loadDraft(draft);

    // Load player pool from Worker
    const seasonId = new URL(location.href).searchParams.get('season');
    const teamAbbrs = await getSeasonTeamAbbrs(seasonId);
    await FADraftRoom.loadPlayerPool(
      teamAbbrs,
      draft.settings.ratingMin,
      draft.settings.ratingMax,
      DNA_CONFIG.ratings.workerUrl
    );

    FADraftRoom.subscribeRealtime(draftId);

    document.getElementById('fa-nav-title').textContent = draft.name || 'FA Draft';
    updateNavStatus(draft.status);

    // Show commissioner controls
    if (_isAdmin()) {
      document.getElementById('fa-controls').style.display = 'flex';
    }

    showScreen('draft');
    updateOnClock();
    renderAvailablePlayers();
    renderDraftBoard();
    renderMyPicks();

    if (draft.status === 'active') {
      FADraftRoom.startTimer();
    }
    if (draft.status === 'completed') {
      document.getElementById('fa-export-tab').style.display = '';
      switchTab('export', document.getElementById('fa-export-tab'));
      renderExportSummary();
    }

    checkYourTurn();
  } catch(e) {
    console.error('enterDraftRoom:', e);
    faToast('Error loading draft: ' + e.message);
    showScreen('loading');
  }
}

async function getSeasonTeamAbbrs(seasonId) {
  const res = await db.from('league_teams')
    .select('mlb_team_id, mlb_teams(abbreviation)')
    .eq('season_id', seasonId)
    .not('mlb_team_id', 'is', null);
  return (res.data || []).map(r => r.mlb_teams?.abbreviation).filter(Boolean);
}

// ── ON-CLOCK / TURN ───────────────────────────────────────
function updateOnClock() {
  const draft = FADraftRoom.getCurrentPick ? { cur: FADraftRoom.getCurrentPick() } : null;
  const cur = FADraftRoom.getCurrentPick?.();
  const nameEl = document.getElementById('fa-on-clock-name');
  const statusEl = document.getElementById('fa-nav-status');

  if (!cur) {
    nameEl.textContent = 'Draft Complete';
    nameEl.className = 'fa-on-clock-name';
    updateNavStatus('completed');
    document.getElementById('fa-export-tab').style.display = '';
    renderExportSummary();
    return;
  }
  nameEl.textContent = cur.memberName;
  nameEl.className = 'fa-on-clock-name' + (cur.memberId === currentMember?.id ? ' your-turn' : '');
}

function checkYourTurn() {
  const cur = FADraftRoom.getCurrentPick?.();
  if (cur && cur.memberId === currentMember?.id) {
    faToast("It's your turn to pick!", 3000);
  }
}

function updateNavStatus(status) {
  const el = document.getElementById('fa-nav-status');
  el.textContent = status.charAt(0).toUpperCase() + status.slice(1);
  el.className = 'fa-nav-status status-' + (status === 'paused' ? 'paused' : status === 'active' ? 'active' : status === 'completed' ? 'complete' : 'setup');
}

function updatePauseBtn(isPaused) {
  const btn = document.getElementById('fa-pause-btn');
  if (btn) btn.textContent = isPaused ? 'Resume' : 'Pause';
}
window.updatePauseBtn = updatePauseBtn;

function togglePause() {
  const btn = document.getElementById('fa-pause-btn');
  if (btn && btn.textContent === 'Pause') {
    FADraftRoom.pauseDraft();
  } else {
    FADraftRoom.resumeDraft();
  }
}

// ── RENDER: AVAILABLE PLAYERS ─────────────────────────────
function filterPlayers(group, btn) {
  currentPosFilter = group;
  document.querySelectorAll('.fa-filter-btn').forEach(b => b.classList.remove('active'));
  btn.classList.add('active');
  renderAvailablePlayers();
}
window.filterPlayers = filterPlayers;

function renderAvailablePlayers() {
  const players = FADraftRoom.getAvailablePlayers(currentPosFilter);
  const canPick  = FADraftRoom.canPick(currentMember?.id);
  const el = document.getElementById('fa-player-list-inner');

  if (!players.length) {
    el.innerHTML = '<div style="padding:2rem;text-align:center;color:var(--text2);font-size:13px;">No available players in this range.</div>';
    return;
  }

  el.innerHTML = players.map((p, idx) => {
    const tier = p.overall >= 85 ? 'diamond' : p.overall >= 80 ? 'gold' : p.overall >= 75 ? 'silver' : p.overall >= 70 ? 'bronze' : 'common';
    return `<div class="fa-player-row" id="fpr-${idx}">
      <div class="fa-player-row-header" onclick="togglePlayerDetail(${idx})">
        <div class="fa-ovr-badge ${tier}">${p.overall}</div>
        <div>
          <div class="fa-player-name">${escHtml(p.name)}</div>
          <div class="fa-player-meta">${escHtml(p.pos)} · ${escHtml(p.fromTeam)} · ${escHtml(p.series||'Live Series')} · ${escHtml(p.rarity||'')}</div>
        </div>
        <button class="fa-expand-btn">▼ Details</button>
      </div>
      <div class="fa-player-detail" id="fpd-${idx}" style="display:none;">
        ${renderPlayerDetail(p, idx, canPick)}
      </div>
    </div>`;
  }).join('');
}
window.renderAvailablePlayers = renderAvailablePlayers;

function togglePlayerDetail(idx) {
  const det = document.getElementById('fpd-' + idx);
  const btn = document.querySelector('#fpr-' + idx + ' .fa-expand-btn');
  const open = det.style.display === 'none';
  det.style.display = open ? '' : 'none';
  if (btn) btn.textContent = open ? '▲ Hide' : '▼ Details';
}
window.togglePlayerDetail = togglePlayerDetail;

function attrColor(val) {
  if (val >= 85) return 'high';
  if (val >= 70) return 'mid';
  if (val >= 55) return 'low';
  return 'vlow';
}

function renderPlayerDetail(p, idx, canPick) {
  const isPitcher = ['SP','RP','CP'].includes(p.pos);
  let html = '';

  if (isPitcher) {
    html += `<div class="fa-attr-section">
      <div class="fa-attr-label">Pitching</div>
      <div class="fa-attr-grid">
        ${[['Velocity', p.velocity],['Control', p.control],['Break', p.break_rating],
           ['Stamina', p.stamina],['K/9', p.k_per_bf],['BB/9', p.bb_per_bf],
           ['HR/9', p.hr_per_bf],['Clutch', p.pitching_clutch]
          ].map(([n,v]) => `<div class="fa-attr-row"><span class="fa-attr-name">${n}</span><span class="fa-attr-val ${attrColor(v||0)}">${v||0}</span></div>`).join('')}
      </div>
    </div>`;
    if (p.pitch_arsenal && p.pitch_arsenal.length) {
      html += `<div class="fa-attr-section">
        <div class="fa-attr-label">Pitch Repertoire</div>
        ${p.pitch_arsenal.map(pitch => `
          <div class="fa-pitch-row">
            <span class="fa-pitch-name">${escHtml(pitch.name)}</span>
            <div class="fa-pitch-stats">MPH <span>${pitch.speed||'—'}</span> BRK <span>${pitch.break||'—'}</span></div>
          </div>`).join('')}
      </div>`;
    }
  } else {
    html += `<div class="fa-attr-section">
      <div class="fa-attr-label">Hitting</div>
      <div class="fa-attr-grid">
        ${[['Contact L', p.contact_left],['Contact R', p.contact_right],
           ['Power L', p.power_left],['Power R', p.power_right],
           ['Vision', p.plate_vision],['Discipline', p.plate_disc],['Clutch', p.clutch]
          ].map(([n,v]) => `<div class="fa-attr-row"><span class="fa-attr-name">${n}</span><span class="fa-attr-val ${attrColor(v||0)}">${v||0}</span></div>`).join('')}
      </div>
    </div>
    <div class="fa-attr-section">
      <div class="fa-attr-label">Speed & Fielding</div>
      <div class="fa-attr-grid">
        ${[['Speed', p.speed],['Stealing', p.stealing],
           ['Fielding', p.fielding],['Arm Strength', p.arm_strength],['Arm Accuracy', p.arm_accuracy]
          ].map(([n,v]) => `<div class="fa-attr-row"><span class="fa-attr-name">${n}</span><span class="fa-attr-val ${attrColor(v||0)}">${v||0}</span></div>`).join('')}
      </div>
    </div>`;
  }

  if (p.quirks && p.quirks.length) {
    html += `<div class="fa-attr-section">
      <div class="fa-attr-label">Quirks</div>
      <div class="fa-quirks">${p.quirks.map(q => `<span class="fa-quirk">${escHtml(q)}</span>`).join('')}</div>
    </div>`;
  }

  html += `<button class="fa-select-btn" ${canPick ? '' : 'disabled'} onclick="pickPlayer(${idx})">Select This Player</button>`;
  return html;
}

function pickPlayer(idx) {
  const players = FADraftRoom.getAvailablePlayers(currentPosFilter);
  if (!players[idx]) return;
  FADraftRoom.makePick(players[idx]);
}
window.pickPlayer = pickPlayer;

// ── RENDER: DRAFT BOARD ───────────────────────────────────
function renderDraftBoard() {
  const draft = window._activeDraft?.();
  // Access draft state via engine — re-read from the engine's getCurrentPick etc.
  // We reconstruct the board from the DOM by re-fetching from DB on each pick.
  // Simpler: re-load from DB when rendering board.
  const el = document.getElementById('fa-board-inner');
  if (!el) return;
  // Re-load draft slots from DB for authoritative board display
  if (!activeDraftId) return;
  db.from('draft_slots')
    .select('pick_number, member_id, skipped, league_members(display_name), draft_picks(player_name, player_rating)')
    .eq('draft_id', activeDraftId)
    .order('pick_number', { ascending: true })
    .then(res => {
      if (res.error || !res.data) return;
      const slots = res.data;
      const cur = FADraftRoom.getCurrentPick?.();

      // Group by round
      const rounds = {};
      slots.forEach(s => {
        const pick = (s.draft_picks && s.draft_picks[0]) ? s.draft_picks[0] : null;
        const round = Math.ceil(s.pick_number / (slots.length / (window._draftRounds || 1)));
        if (!rounds[round]) rounds[round] = [];
        rounds[round].push({ ...s, pick });
      });

      el.innerHTML = Object.keys(rounds).map(r => `
        <div class="fa-board-round">
          <div class="fa-board-round-label">Round ${r}</div>
          ${rounds[r].map(s => {
            const isCur = cur && cur.pickNumber === s.pick_number;
            const pick = s.pick;
            return `<div class="fa-board-pick ${isCur ? 'on-clock' : ''} ${pick ? 'done' : ''}">
              <div class="fa-board-pick-num">${s.pick_number}</div>
              <div class="fa-board-pick-member">${escHtml(s.league_members?.display_name || '—')}</div>
              ${pick ? `<div class="fa-board-pick-result">${escHtml(pick.player_name)} <span style="color:var(--text2);font-size:11px;">${pick.player_rating} OVR</span></div>` : ''}
            </div>`;
          }).join('')}
        </div>`).join('');
    });
}
window.renderDraftBoard = renderDraftBoard;

// ── RENDER: MY PICKS ──────────────────────────────────────
function renderMyPicks() {
  const el = document.getElementById('fa-mypicks-inner');
  if (!activeDraftId || !currentMember) return;
  db.from('draft_picks')
    .select('player_name, player_rating, picked_at, mlb_team_id, mlb_teams(abbreviation)')
    .eq('draft_id', activeDraftId)
    .eq('member_id', currentMember.id)
    .order('pick_number', { ascending: true })
    .then(res => {
      const picks = res.data || [];
      if (!picks.length) {
        el.innerHTML = '<div style="padding:2rem;text-align:center;color:var(--text2);font-size:13px;">No picks yet.</div>';
        return;
      }
      el.innerHTML = picks.map(p => {
        const tier = p.player_rating >= 85 ? 'diamond' : p.player_rating >= 80 ? 'gold' : p.player_rating >= 75 ? 'silver' : p.player_rating >= 70 ? 'bronze' : 'common';
        return `<div class="fa-player-row" style="margin-bottom:6px;">
          <div class="fa-player-row-header" style="cursor:default;">
            <div class="fa-ovr-badge ${tier}">${p.player_rating}</div>
            <div>
              <div class="fa-player-name">${escHtml(p.player_name)}</div>
              <div class="fa-player-meta">From: ${escHtml(p.mlb_teams?.abbreviation||'—')} · ${p.picked_at ? new Date(p.picked_at).toLocaleString() : ''}</div>
            </div>
          </div>
        </div>`;
      }).join('');
    });
}
window.renderMyPicks = renderMyPicks;

// ── RENDER: EXPORT SUMMARY ────────────────────────────────
function renderExportSummary() {
  const rows = FADraftRoom.getExportData();
  const el = document.getElementById('fa-export-summary');
  if (!rows.length) { el.innerHTML = ''; return; }
  el.innerHTML = `<div style="margin-top:1.5rem;">
    <div style="font-family:'Barlow Condensed';font-size:11px;letter-spacing:3px;text-transform:uppercase;color:var(--text2);margin-bottom:0.75rem;">${rows.length} Picks</div>
    ${rows.map(r => `<div style="background:var(--surface2);border-radius:8px;padding:10px 14px;margin-bottom:6px;font-size:13px;">
      <strong>${escHtml(r.playerName)}</strong>
      <span style="color:var(--text2);margin:0 6px;">·</span>${r.playerOvr} OVR · ${escHtml(r.playerPos)}
      <span style="color:var(--text2);margin:0 6px;">·</span>From: ${escHtml(r.originalTeam)}
      <span style="color:var(--text2);margin:0 6px;">→</span>${escHtml(r.draftedBy)}
    </div>`).join('')}
  </div>`;
}

function showRecap() {
  document.getElementById('fa-export-tab').style.display = '';
  switchTab('export', document.getElementById('fa-export-tab'));
  renderExportSummary();
  updateOnClock();
}
window.showRecap = showRecap;

// ── TAB SWITCHING ─────────────────────────────────────────
function switchTab(name, btn) {
  document.querySelectorAll('.fa-tab').forEach(t => t.classList.remove('active'));
  document.querySelectorAll('.fa-tab-content').forEach(t => t.classList.remove('active'));
  btn.classList.add('active');
  document.getElementById('tab-' + name).classList.add('active');
  if (name === 'board') renderDraftBoard();
  if (name === 'mypicks') renderMyPicks();
}
window.switchTab = switchTab;

// ── SCREENS ───────────────────────────────────────────────
function showScreen(name) {
  ['auth','loading','setup','draft'].forEach(s => {
    const el = document.getElementById('screen-' + s);
    if (el) el.classList.toggle('show', s === name);
  });
}

// ── BOOT ─────────────────────────────────────────────────
boot();
</script>
</body>
</html>
```

- [ ] **Step 2: Manual smoke test**

Start the local server (`python -m http.server 8000`), navigate to `http://localhost:8000/fa-draft.html?season=<a-valid-season-id>`.

Expected as admin: setup screen shows with tier buttons and draft order preview.
Expected as member: "No FA draft created yet" toast.

- [ ] **Step 3: Commit**

```bash
git add fa-draft.html
git commit -m "feat: add fa-draft.html standalone FA draft room"
```

---

## Task 5: Seasons page — "FA Draft" button

**Files:**
- Modify: `index.html` — `renderSeasonCard()` function

- [ ] **Step 1: Add "FA Draft" button to each season card**

In `index.html`, find the `teamsContent` section that ends with the `autoLoadDraftTeams` button (around line 2177). After the closing `'</div>';` of `teamsContent +=`, find where `teamsContent` closes:

```js
    teamsContent += '<div style="margin-top:12px;display:flex;gap:8px;flex-wrap:wrap;">' +
      (hasDraftData
        ? '<button type="button" class="btn btn-outline btn-sm" style="color:var(--gold);border-color:rgba(201,168,76,0.3);" onclick="autoLoadDraftTeams(\'' + s.id + '\')">&#x26A1; Auto-load from Draft</button>'
        : '') +
      '</div>';
```

Replace with:

```js
    // Check if FA draft exists for this season
    var hasFaDraft = false; // will be async-resolved separately
    teamsContent += '<div style="margin-top:12px;display:flex;gap:8px;flex-wrap:wrap;">' +
      (hasDraftData
        ? '<button type="button" class="btn btn-outline btn-sm" style="color:var(--gold);border-color:rgba(201,168,76,0.3);" onclick="autoLoadDraftTeams(\'' + s.id + '\')">&#x26A1; Auto-load from Draft</button>'
        : '') +
      (canManageSchedule() && Object.keys(teams).length > 0
        ? '<button type="button" id="fa-draft-btn-' + s.id + '" class="btn btn-outline btn-sm" style="color:var(--blue);border-color:rgba(106,158,199,0.4);" onclick="openFADraft(\'' + s.id + '\')">&#x1F3CB; FA Draft</button>'
        : '') +
      '</div>';
```

Then add `openFADraft` function after `renderSeasonCard`:

```js
function openFADraft(seasonId) {
  window.open('fa-draft.html?season=' + seasonId, '_blank');
}
```

- [ ] **Step 2: Verify button appears**

Open the Seasons page for a season with team assignments. The "FA Draft" button should appear next to "Auto-load from Draft" in the Teams tab. Clicking it should open a new tab to `fa-draft.html?season=<id>`.

- [ ] **Step 3: Commit**

```bash
git add index.html
git commit -m "feat: add FA Draft button to season card teams tab"
```

---

## Task 6: Update CLAUDE.md

**Files:**
- Modify: `CLAUDE.md`

- [ ] **Step 1: Add FA draft section to CLAUDE.md**

In `CLAUDE.md`, under `## Cloudflare Worker Routes`, add the new route:

```markdown
| `GET /fa-roster?team=LAD&min=70&max=84` | All Live Series players for a team in OVR range, full attributes | 1 hour |
```

Under `## Features Built`, add:

```markdown
| — | `feature/fa-draft` | Standalone FA draft room (`fa-draft.html`). Reversed snake order from team draft, configurable rating range with tier buttons, full player attribute cards (hitting/pitching/pitch arsenal/quirks), CSV + printable trade checklist export. |
```

Under `## Key Patterns & Gotchas`, add:

```markdown
- **FA draft entry:** `fa-draft.html?season=<id>` — linked from the season card Teams tab (commissioner+ only when teams are assigned). Config screen shown only to admin/commissioner if no FA draft exists yet.
- **FA draft player pool:** Loaded from Worker `/fa-roster` for each team in `league_teams` for the season. Pitcher cards include pitch repertoire (`pitch_arsenal` array). Trade return player selection matches position group (P→P, C→C, IF→IF, OF→OF), falling back to absolute lowest if same-group candidate is within 5 OVR of the drafted player.
```

- [ ] **Step 2: Commit**

```bash
git add CLAUDE.md
git commit -m "docs: update CLAUDE.md with FA draft details"
```

---

## Task 7: Final integration test

- [ ] **Step 1: Full flow test (commissioner)**

1. Open the app, go to Seasons, open a season with team assignments
2. Click "FA Draft" — new tab opens to `fa-draft.html?season=<id>`
3. Setup screen shows: tier buttons, min/max inputs, rounds, timer, order preview
4. Click "Gold" tier → min=80, max=84 fills in
5. Manually change min to 70 — Gold button un-highlights
6. Set rounds=1, timer=2 min → click "Launch FA Draft"
7. Draft room appears — on-clock header shows first member
8. Available Players tab loads players with OVR 70–84
9. Click "▼ Details" on a hitter → attributes section expands with hitting/fielding attrs and quirks
10. Click "▼ Details" on a pitcher → pitching attrs + pitch repertoire rows show
11. Click "Select This Player" → pick is recorded, player disappears from list, draft board updates
12. Pause → Resume works
13. Skip → advances to next slot
14. Complete all picks (or End Draft Early) → Export tab appears
15. "Print Trade Checklist" → new tab with grouped trade rows, checkboxes, hints
16. "Download CSV" → file downloads with correct columns

- [ ] **Step 2: Multi-user test (member)**

1. Sign in as a regular member in a separate browser tab
2. Navigate to `fa-draft.html?season=<id>`
3. Draft room loads (no setup screen, no commissioner controls)
4. When it's their turn: "It's your turn to pick!" toast appears, "Select This Player" button is enabled
5. When it's not their turn: "Select This Player" is disabled
6. Commissioner pick in first tab appears in member's tab within ~2 seconds (Realtime)

- [ ] **Step 3: Rejoin test**

1. Close the draft room tab mid-draft
2. Reopen `fa-draft.html?season=<id>`
3. Full current state loads from DB — picks so far visible, correct on-clock member shown

- [ ] **Step 4: Push branch for PR**

```bash
git push origin feature/fa-draft
```

Open PR on GitHub: `feature/fa-draft` → `staging`

---

## Self-Review Notes

**Spec coverage check:**
- ✅ Standalone `fa-draft.html` — Task 4
- ✅ Entry point from Seasons page — Task 5
- ✅ No schema changes — reuses `drafts`/`draft_slots`/`draft_picks` with `type='player'`
- ✅ Config screen: tier buttons, min/max, rounds, timer, order preview — Task 4
- ✅ Reversed snake order encoded in slots at launch — Task 4 (`launchFADraft`)
- ✅ Tabbed UI: Players / Board / My Picks — Task 4
- ✅ Full attribute cards: hitter fields, pitcher fields, pitch arsenal, quirks — Tasks 3+4
- ✅ Commissioner controls: pause/resume/skip/undo/end — Tasks 3+4
- ✅ Realtime: broadcast channel `fa_draft:<id>` — Task 3
- ✅ Reconnect: `loadDraftFromDB` rebuilds full state — Task 3
- ✅ Export: CSV download + printable HTML checklist — Task 3
- ✅ Trade return: position-group matching with 5-OVR fallback — Task 3 (`_findTradeReturn`)
- ✅ Worker `/fa-roster` route — Task 1
- ✅ All members can join, pick only when on clock — Tasks 3+4
- ✅ One FA draft per season (commissioner sees setup only if none exists) — Task 4
